__pycache__/
*.pyc

# makepkg build artifacts
packaging/pkg/
packaging/*.pkg.tar.*

# packaging/src/ holds real, hand-written helper source (dualsense-hidlock.c)
# that IS committed - only ignore actual build output that might land there.
packaging/src/*.o

# Decky plugin build tooling - dist/ IS committed (install.sh needs it, and
# most SteamOS installs have no Node toolchain to build it themselves)
deck-plugin/node_modules/
